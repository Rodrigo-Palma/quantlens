# Copy to .env and fill in. Never commit .env.
# Default: local LLM via Ollama (offline, no API key needed).
LLM_PROVIDER=ollama
LLM_BASE_URL=http://localhost:11434
LLM_MODEL=qwen3:32b

# Required only when switching to a paid API provider (anthropic / openai):
LLM_API_KEY=
